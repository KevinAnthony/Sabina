<Window x:Class="Noside.Wyvern"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Noside.CoinCounter.Controls"
        Style="{StaticResource NsdWindowStyle}"
        Loaded="OnWindowLoaded" Closing="OnWindowClosing"  MouseDown="OnDragMouseDown"
        Title="Coin Counter Mk 4" WindowStartupLocation="CenterScreen">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Grid.Column="0" x:Name="TitleBar" MouseDown="OnDragMouseDown">
            <Button x:Name="CloseButton" Style="{StaticResource NsdWindowButtonStyle}"
                    Content="X"
                    Click="CloseButton_Click" FontFamily="Segoe UI Symbol" />
            <Button x:Name="MinButton" Style="{StaticResource NsdWindowButtonStyle}"
                    Content="{StaticResource MinimizeWindow}" Click="MinButton_Click" />
                <TextBlock VerticalAlignment="Center" TextAlignment="Left"
                       Text="{Binding Title, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                       Margin="7,0,0,0" MouseDown="OnDragMouseDown" />
            </DockPanel>
            <controls:MainView Grid.Row="1" Grid.Column="0" x:Name="CoinView" />
        </Grid>
    
</Window>
